==只会判断取值是否相等，不会判断数据类型是否相等
===不仅会判断取值是否相等，还会判断数据类型是否相等；只要数据类型跟值都相等才会返回true

!=只会判断值是否不等，不会判断数据类型是否不等；
!==会判断值跟数据类型是否都不相等，如果数据类型不同，那么就返回true;只有在数据类型跟值同时不同的时候
才会返回true;

关系运算符中> < >= <=的优先级高于== != === !==

逻辑运算符： && || !

在逻辑运算中，逻辑与&&的优先级高于逻辑或||

逗号运算符：优先级是所有运算符中最低的；逗号运算符的结果就是最后一个表达式的结果；
